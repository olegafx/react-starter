import React, { Component } from 'react';

import Test from './Test';

export default class Root extends Component {
  constructor (props) {
    super(props);
  }

  render () {
    return (
      <Test/>
    );
  }
}
